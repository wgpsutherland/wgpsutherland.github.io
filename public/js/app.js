define([
    'underscore',
    'jquery',
    'backbone',
    'router'
], function(_, $, Backbone, Router) {
    return {
        initialize: function() {
            Router.initialize();
        }
    };
});